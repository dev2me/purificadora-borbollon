<%= form_for(order) do |f| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "tipo de pedido" %>
    <%= f.text_field :pedido %>
  </div>

  <div class="field">
    <%= f.label "Fecha de entrada" %>
    <%= f.date_select :fecha_entrada %>
  </div>

  <div class="field">
    <%= f.label "Fecha de salida" %>
    <%= f.date_select :fecha_salida %>
  </div>

  <div class="field">
    <%= f.label "Id del cliente" %>
    <%= f.number_field :client_id %>
  </div>

  <div class="actions">
    <%= f.submit "Crear pedido"%>
  </div>
<% end %>
